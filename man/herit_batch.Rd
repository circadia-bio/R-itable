% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/herit_batch.R
\name{herit_batch}
\alias{herit_batch}
\title{Batch heritability estimation over multiple traits}
\usage{
herit_batch(
  traits,
  grm,
  data,
  covs_list = NULL,
  id_col = "IID",
  min_n = 80L,
  ci_level = 0.95,
  .progress = TRUE
)
}
\arguments{
\item{traits}{Character vector of trait column names in \code{data}.}

\item{grm}{Numeric matrix: additive genetic relationship matrix as returned
by \code{\link[=build_grm]{build_grm()}}.}

\item{data}{Data frame containing ID, trait, and covariate columns.}

\item{covs_list}{A named list of covariate vectors, where each element
defines one covariate model. If \code{NULL}, a single unadjusted model is run.
Example: \code{list(unadj = NULL, cov1 = c("age", "sex"), cov2 = c("age", "sex", "age2"))}.}

\item{id_col}{Name of the individual ID column. Default \code{"IID"}.}

\item{min_n}{Minimum sample size to attempt estimation. Default \code{80}.}

\item{ci_level}{Profile-likelihood CI level. Default \code{0.95}.}

\item{.progress}{Logical. Show a cli progress bar. Default \code{TRUE}.}
}
\value{
A data frame (tibble-compatible) with one row per successfully
fitted model and columns: \code{label}, \code{trait}, \code{covariates}, \code{n}, \code{h2},
\code{se}, \code{ci_lo}, \code{ci_hi}, \code{pval}, \code{sigma2_a}, \code{sigma2_e}.
Failed / skipped models are silently omitted.
}
\description{
A convenience wrapper around \code{\link[=herit_vc]{herit_vc()}} that iterates over a vector of
trait names, optionally across multiple covariate models, and returns a
tidy data frame.
}
\examples{
\dontrun{
A    <- build_grm(my_pedigree, study_ids = my_data$IID)

res  <- herit_batch(
  traits     = c("bmi", "systolic_bp", "hdl"),
  grm        = A,
  data       = my_data,
  covs_list  = list(
    unadj = NULL,
    cov1  = c("age", "sex"),
    cov2  = c("age", "sex", "age2")
  )
)

# Significant adjusted models
subset(res, grepl("cov2", label) & pval < 0.05)
}

}
\seealso{
\code{\link[=herit_vc]{herit_vc()}}, \code{\link[=build_grm]{build_grm()}}, \code{\link[=plot_forest]{plot_forest()}}
}
