% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_household.R
\name{build_household}
\alias{build_household}
\title{Build a household (shared-environment) indicator matrix}
\usage{
build_household(hhid, ids)
}
\arguments{
\item{hhid}{Vector of household group labels, one per individual, in the
same order as \code{ids}. Individuals sharing an \code{hhid} value are treated as
co-resident (e.g. twin pairs reared together).}

\item{ids}{Vector of individual IDs, the same length as \code{hhid}. Used to
name the rows/columns of the returned matrix so it aligns with a GRM
from \code{\link[=build_grm]{build_grm()}}.}
}
\value{
A symmetric numeric matrix of dimension \verb{length(ids) x length(ids)}, with row/column names \code{as.character(ids)}.
}
\description{
Constructs the block-diagonal indicator matrix \strong{C} used as the shared
/common-environment ("household") random-effect design in \code{\link[=herit_ace]{herit_ace()}}:
\code{C[i, j] = 1} if individuals \code{i} and \code{j} belong to the same household
group, \code{0} otherwise (including the diagonal convention \code{C[i, i] = 1}).
}
\examples{
hh  <- c("A", "A", "B", "B", "C")
ids <- 1:5
build_household(hh, ids)

}
