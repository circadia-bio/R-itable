% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest plot of heritability estimates}
\usage{
plot_forest(
  results,
  model_filter = NULL,
  colour_by = "trait",
  sig_threshold = 0.05,
  title = NULL,
  x_limits = c(0, 1)
)
}
\arguments{
\item{results}{Data frame as returned by \code{\link[=herit_batch]{herit_batch()}}, containing at
least columns \code{label}, \code{trait}, \code{h2}, \code{ci_lo}, \code{ci_hi}, \code{pval}.}

\item{model_filter}{Optional character vector of model name substrings to
keep (matched against \code{label}). E.g. \code{"cov2"} to show only the
age + sex + age-squared model.}

\item{colour_by}{Column to colour points by. Default \code{"trait"}. Set to
\code{NULL} for a monochrome plot.}

\item{sig_threshold}{Numeric. Traits with \code{pval} below this threshold are
shown with a filled point; others with an open point. Default \code{0.05}.}

\item{title}{Optional plot title string.}

\item{x_limits}{Numeric vector of length 2 for the x-axis range.
Default \code{c(0, 1)}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Produces a ggplot2 forest plot from the output of \code{\link[=herit_batch]{herit_batch()}} or a
list of \code{\link[=herit_vc]{herit_vc()}} results coerced to a data frame.
}
\details{
Requires \strong{ggplot2} (listed in \code{Suggests}). An informative error is thrown
if it is not installed.
}
\examples{
\dontrun{
res <- herit_batch(c("bmi", "hdl", "systolic_bp"),
                   grm = A, data = my_data,
                   covs_list = list(unadj = NULL,
                                    cov2  = c("age", "sex", "age2")))

plot_forest(res, model_filter = "cov2", title = "Adjusted heritability")
}

}
\seealso{
\code{\link[=herit_batch]{herit_batch()}}
}
