% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/herit_bivar.R
\name{herit_bivar}
\alias{herit_bivar}
\title{Bivariate genetic and environmental correlation between two traits}
\usage{
herit_bivar(
  trait1,
  trait2,
  grm,
  data,
  id_col = "IID",
  transform = TRUE,
  min_n = 80L,
  verbose = TRUE
)
}
\arguments{
\item{trait1, trait2}{Character strings: names of the two trait columns in
\code{data}.}

\item{grm}{Numeric matrix: additive genetic relationship matrix, as
returned by \code{\link[=build_grm]{build_grm()}}.}

\item{data}{Data frame containing \code{id_col}, \code{trait1}, and \code{trait2}.}

\item{id_col}{Name of the individual ID column in \code{data}. Default \code{"IID"}.}

\item{transform}{Logical. Apply \code{\link[=int_transform]{int_transform()}} to both traits before
fitting. Default \code{TRUE}.}

\item{min_n}{Minimum number of individuals contributing to the analysis
sample required (an individual counts if at least one of \code{trait1},
\code{trait2} is observed -- see Details). Default \code{80L}.}

\item{verbose}{Logical. Print progress via \code{cli}. Default \code{TRUE}.}
}
\value{
A named list with elements:
\describe{
\item{\code{trait1}, \code{trait2}}{Trait names.}
\item{\code{n}}{Number of individuals contributing to the analysis (union of
those with \code{trait1} and/or \code{trait2} observed -- see Details).}
\item{\code{n_complete}}{Number of individuals with both traits observed.
Equal to \code{n} when neither trait has missing data.}
\item{\code{h2_1}, \code{h2_2}}{Heritability of each trait under the joint
bivariate model.}
\item{\code{rhoG}, \code{rhoE}, \code{rhoP}}{Genetic, environmental, and derived
phenotypic correlations.}
\item{\code{p_rhoG}, \code{p_rhoE}}{LRT p-values for rhoG = 0 and rhoE = 0
(standard two-sided chi-squared(1); these correlations are not
boundary parameters).}
\item{\code{p_rhoP}}{LRT p-value for the joint null rhoG = rhoE = 0
(chi-squared(2)).}
}
Returns \code{NULL} if \code{n < min_n}.
}
\description{
Fits a bivariate AE variance-components model for two quantitative traits
and estimates the additive-genetic correlation (rhoG), the non-shared
environmental correlation (rhoE), and the derived phenotypic correlation
(rhoP = rhoG\emph{sqrt(h2_1}h2_2) + rhoE\emph{sqrt((1-h2_1)}(1-h2_2))), together
with likelihood-ratio tests of rhoG = 0, rhoE = 0, and the joint test of
rhoP = 0 (rhoG = rhoE = 0). This mirrors SOLAR Eclipse's
\code{polygenic -testrhog} / \code{-testrhoe} bivariate output (see
\code{solar_full_analysis.tcl}, Part 5).
}
\details{
\strong{Model:} traits are stacked into a \verb{2n}-vector with block covariance
\verb{Sigma = [[sigma2_g1*A + sigma2_e1*I, rhoG*sqrt(sigma2_g1*sigma2_g2)*A + rhoE*sqrt(sigma2_e1*sigma2_e2)*I], [..., sigma2_g2*A + sigma2_e2*I]]},
where \code{sigma2_g_i = h2_i * sigma2_p_i} and \code{sigma2_e_i = (1-h2_i) * sigma2_p_i}. All six free parameters (\verb{h2_1, h2_2, rhoG, rhoE, sigma2_p1, sigma2_p2}) are estimated jointly by multi-start Nelder-Mead ML on a
reparametrised scale (logit for \code{h2}, \code{atanh} for the correlations, \code{log}
for the variances) so that all constraints hold automatically.

\strong{Missing data:} if either trait has missing values, individuals missing
exactly one of the two traits are still included in the analysis sample,
contributing their observed trait's marginal likelihood, matching SOLAR
Eclipse's default \code{UnbalancedTraits} behaviour (individuals are only
excluded if \emph{both} traits, or the ID, are missing). This is both more
statistically appropriate than dropping them (more information used under
the usual MAR assumption) and matches SOLAR exactly on affected pairs once
combined with the SOLAR-exact \code{inormal} tie handling in \code{\link[=int_transform]{int_transform()}}
(see NEWS.md for validation). When neither trait has missing data (the
common case), this reduces to the same fast eigenbasis computation as
before -- the unbalanced path is only used when needed, since it cannot
exploit the eigenbasis shortcut (the missingness pattern is individual-
specific, so the joint covariance no longer shares a common eigenbasis
with \code{A} across all n individuals).

As with \code{\link[=herit_ace]{herit_ace()}}, the unbalanced path uses direct numerical ML over
the full covariance matrix rather than an eigendecomposition shortcut.

Validated on the Gomes et al. twin dataset (31 trait pairs), combining
correct per-trait inverse-normal transform selection, SOLAR-exact
\code{inormal} tie handling (see \code{\link[=int_transform]{int_transform()}}), data-driven optimiser
starting values, and unbalanced-trait handling: 29 of 31 pairs match
SOLAR Eclipse to 4 decimal places exactly on both rhoG and rhoE, and all
31 pairs agree on nominal significance for both. The bivariate model's
omega was also independently confirmed algebraically identical to
SOLAR's own documented formula for multivariate polygenic models (SOLAR
Manual Chapter 9). The 2 remaining pairs both involve a single phenotype
with an anomalous raw variance (a data-quality issue independent of this
package, not a modelling gap) -- see NEWS.md.
}
\seealso{
\code{\link[=build_grm]{build_grm()}}, \code{\link[=herit_vc]{herit_vc()}}, \code{\link[=herit_ace]{herit_ace()}}, \code{\link[=herit_bivar_batch]{herit_bivar_batch()}}
}
