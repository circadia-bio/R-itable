% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/herit_vc.R
\name{herit_vc}
\alias{herit_vc}
\title{Profile-likelihood variance-components heritability estimation}
\usage{
herit_vc(
  trait,
  grm,
  data,
  covs = NULL,
  id_col = "IID",
  label = NULL,
  min_n = 80L,
  ci_level = 0.95,
  verbose = TRUE
)
}
\arguments{
\item{trait}{Character string: name of the trait column in \code{data}.}

\item{grm}{Numeric matrix: the additive genetic relationship matrix for
all individuals in \code{data}, as returned by \code{\link[=build_grm]{build_grm()}}. Row and column
names must match the ID column of \code{data}.}

\item{data}{Data frame containing \code{id_col}, \code{trait}, and any covariate
columns.}

\item{covs}{Character vector of covariate column names, or \code{NULL} for an
intercept-only model. Covariates are mean-centred and scaled to unit
variance before fitting.}

\item{id_col}{Name of the individual ID column in \code{data}. Default \code{"IID"}.}

\item{label}{Optional string label for this model (used in batch output).
If \code{NULL}, defaults to \code{"<trait>_adj"} or \code{"<trait>_unadj"}.}

\item{min_n}{Minimum number of complete observations required to attempt
estimation. Models with fewer observations return \code{NULL} silently.
Default \code{80}.}

\item{ci_level}{Confidence level for the profile-likelihood interval.
Default \code{0.95}.}

\item{verbose}{Logical. Print progress to the console. Default \code{TRUE}.}
}
\value{
A named list with elements:
\describe{
\item{\code{label}}{Model label.}
\item{\code{trait}}{Trait name.}
\item{\code{covariates}}{Covariate names joined by \code{"+"}, or \code{""}.}
\item{\code{n}}{Sample size after dropping missing values.}
\item{\code{h2}}{MLE of narrow-sense heritability.}
\item{\code{se}}{Standard error from profile-likelihood curvature (Wald).}
\item{\code{ci_lo}, \code{ci_hi}}{Profile-likelihood confidence interval bounds.}
\item{\code{pval}}{One-sided LRT p-value with chi-squared(1) boundary correction.}
\item{\code{var_covariates}}{Proportion of phenotypic variance explained by
fixed-effect covariates (R² on INT-transformed phenotype). \code{NA} for
unadjusted models. Corresponds to the "variance explained" column in
Leocadio-Miguel et al. (2025).}
\item{\code{sigma2_a}}{Additive genetic variance (sigma²_g in SOLAR notation).}
\item{\code{sigma2_e}}{Residual environmental variance (sigma²_e).}
}
Returns \code{NULL} if \code{n < min_n} or if the GRM subset is degenerate.
}
\description{
Estimates narrow-sense heritability (h\ifelse{html}{\out{&sup2;}}{\eqn{^2}})
for a single quantitative trait using a profile-likelihood variance-components
approach equivalent to SOLAR Eclipse. The phenotype is inverse-normal
transformed internally.
}
\details{
\strong{Model:} Omega = sigma2_p [h2 A + (1 - h2) I_n]

\strong{Optimisation:} eigendecomposition of A followed by 1-D
profile-likelihood optimisation over h2 in (0, 1) using a coarse grid
to seed \code{\link[stats:optimize]{stats::optimize()}}.

\strong{LRT:} one-sided chi-squared(1) boundary correction -- the null is on the
boundary of the parameter space (h2 = 0), so the p-value is halved relative
to a standard chi-squared test. This matches the SOLAR Eclipse convention.

\strong{CIs:} derived by \code{uniroot()} on the profile log-likelihood, falling
back to a Wald interval if the root-finding fails.
}
\examples{
\dontrun{
# Build GRM then estimate heritability
A   <- build_grm(my_pedigree, study_ids = my_data$IID)
res <- herit_vc("bmi", grm = A, data = my_data,
                covs = c("age", "sex", "age2"))
str(res)
}

}
\seealso{
\code{\link[=build_grm]{build_grm()}}, \code{\link[=herit_batch]{herit_batch()}}, \code{\link[=int_transform]{int_transform()}}
}
