% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/herit_bivar.R
\name{herit_bivar_batch}
\alias{herit_bivar_batch}
\title{Batch bivariate genetic/environmental correlations with FDR correction}
\usage{
herit_bivar_batch(
  pairs,
  grm,
  data,
  id_col = "IID",
  transform = TRUE,
  min_n = 80L,
  .progress = TRUE
)
}
\arguments{
\item{pairs}{A two-column character matrix or data frame of trait name
pairs (column 1 = trait1, column 2 = trait2), or a list of length-2
character vectors.}

\item{grm}{Numeric matrix: additive genetic relationship matrix, as
returned by \code{\link[=build_grm]{build_grm()}}.}

\item{data}{Data frame containing \code{id_col}, \code{trait1}, and \code{trait2}.}

\item{id_col}{Name of the individual ID column in \code{data}. Default \code{"IID"}.}

\item{transform}{Logical. Apply \code{\link[=int_transform]{int_transform()}} to both traits before
fitting. Default \code{TRUE}.}

\item{min_n}{Minimum number of individuals contributing to the analysis
sample required (an individual counts if at least one of \code{trait1},
\code{trait2} is observed -- see Details). Default \code{80L}.}

\item{.progress}{Logical. Show a cli progress bar. Default \code{TRUE}.}
}
\value{
A data frame with one row per successfully fitted pair and columns
\code{trait1}, \code{trait2}, \code{n}, \code{n_complete}, \code{h2_1}, \code{h2_2}, \code{rhoG}, \code{rhoE},
\code{rhoP}, \code{p_rhoG}, \code{p_rhoE}, \code{p_rhoP}, \code{q_rhoG}, \code{q_rhoE}, \code{q_rhoP}
(BH-adjusted). Failed / skipped pairs are silently omitted.
}
\description{
Iterates \code{\link[=herit_bivar]{herit_bivar()}} over a set of trait pairs and applies
Benjamini-Hochberg FDR correction separately within each of the three
p-value families (rhoP, rhoG, rhoE), matching \code{parse_and_fdr.py}'s
post-processing of SOLAR output.
}
\seealso{
\code{\link[=herit_bivar]{herit_bivar()}}
}
