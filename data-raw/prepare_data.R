# Script to generate any internal package data (none yet).
# Run this manually via: source("data-raw/prepare_data.R")
#
# When internal test data is added later, use:
#   usethis::use_data(my_object, internal = TRUE)
